# 汇金证金持仓追踪 · 数据刷新使用说明

## 快速开始（推荐）

- **启动**：双击 `start_server.bat`（或命令行运行 `python serve.py`）
- **关闭**：双击 `stop_server.bat`（自动结束占用 8300 端口的服务进程）

启动后自动打开 `http://127.0.0.1:8300/`，点击页面右上角 **「刷新数据」** 按钮即可一键拉取最新真实数据并热更新图表（无需刷新页面）。

## 两种运行模式

| 模式 | 打开方式 | 关闭方式 | 刷新方式 |
|---|---|---|---|
| 本地服务模式 | 双击 `start_server.bat` 或 `python serve.py` | 双击 `stop_server.bat`，或在命令行窗口按 Ctrl+C | 页面内点「刷新数据」一键刷新 |
| 纯文件模式 | 双击 `index.html` | 直接关浏览器标签页即可（无服务进程） | 命令行运行 `python refresh_data.py`，再手动刷新页面 |

> 若发布到 GitHub Pages 等静态托管：页面本身可正常浏览（纯静态、相对路径），但「刷新数据」按钮依赖本地 `/api/refresh` 接口，静态托管下不可用；需在本地刷新后重新提交 `data.js` / `data.json`。

## 追踪范围

当前追踪 **42 只 ETF**（宽基 23 + 行业/主题 19），全部为经十大持有人数据核实、"国家队"四主体（**中央汇金投资、中央汇金资管、国新投资、中国诚通**）持有的 ETF。

- 清单在 `refresh_data.py` 的 `ETFS` 列表中维护（代码、名称、分类、基准指数、管理人）。
- 2026-07 曾对沪深全市场 **14960 只**做过穷举扫描（`scan_market.py`），命中结果存于 `scan_candidates.json`；其中**诚通系 15 只**暂未纳入追踪，仅在页面底部小字备注列出。
- 新增 ETF 时需同步：`ETFS` 清单、`INDEX_SECIDS`（基准指数映射）、`disclosures.json`（披露数据），然后跑一次 `--backfill` 全量回补份额。

## 数据来源（全部为公开接口）

| 数据 | 主源 | 备源 |
|---|---|---|
| ETF 日线（价格、成交额） | **新浪财经**（成交额=量×均价估算，与东财真实值误差<0.01%） | 东方财富 → 腾讯行情 |
| 指数收盘价 | **新浪财经** | 东方财富 → 腾讯行情 |
| 指数成交额 | 搜狐历史行情（真实值，GBK jsonp，单位万元） | 东方财富（缺失时下轮自动重试） |
| ETF 每日总份额 | 上交所 `query.sse.com.cn`（沪市，全量历史）/ 深交所 `investor.szse.cn` `fund_jjgm`（深市，全量历史，分段查询+翻页） | — |
| 国家队定期披露 | `disclosures.json`（新浪十大持有人接口抓取 + 交易所官方份额/净值核算） | — |

## 国家队披露数据口径（重要）

- 分类依据为各 ETF **年报/半年报**十大持有人中四主体的合计持股比例。ETF 十大持有人**只在年报和半年报披露**（季报不披露），当前最新报告期为 **2025-12-31（2025 年年报）**。
- ⚠️ **新浪十大持有人接口的 `date` 参数无效**：无论传入什么日期，返回的都是最近一期已披露定期报告。使用时必须用"披露份额 ÷ 交易所官方当日总份额 = 披露占比"交叉验证真实报告期，不能直接采信传入的日期。
- **持仓金额** = 披露的精确持有份额 × 报告期末官方单位净值（新浪净值接口），非估算。
- **图表数据**：价格、成交额、申赎份额、ETF 总份额均为该 ETF 自身的市场公开数据（非仅国家队的买卖数据）。
- **"国家队未持仓"标注**：表示已查证该报告期十大持有人中不含四主体（非"未查"）。

## 图表说明

- **日线价格走势**：蓝实线 = ETF 前复权价；灰虚线 = 基准指数（独立 min-max 标尺，仅作形态对照，纵轴刻度只对应蓝线）。个别 ETF（515050/560170/159562/159852）的基准指数在当前环境无行情源，只显示蓝线。
- **份额走势图上的标点**：披露日标记——竖虚线为报告期位置，圆点钉在当日总份额上，标签显示该期**国家队合计持仓比例**（低频披露锚点 vs 日频份额曲线的对照）。

## 界面操作

- **日夜间模式**：右上角工具栏点击「夜间」/「日间」按钮切换。首次打开跟随系统偏好，之后自动记住你的选择（`localStorage`）。
- **区间趋势排序**：趋势追踪表格默认把合计行排在最前，且同一分组内按日期**从新到旧**排列（最近的日期在最上方）。

## 刷新脚本

```bash
python refresh_data.py                # 增量刷新（K线增量 + 份额只补缺失日期）
python refresh_data.py --backfill 619 # 全量回补真实份额（新增沪市 ETF 后必跑，约 25 分钟）
```

- **增量缓存**：`kline_cache.json`（K线）、`shares_cache.json`（份额）；重复运行只拉新增日期，速度快且不易触发限流。**勿随意删除**——删除后需重跑约 25 分钟的全量份额回补。
- **份额说明**：沪市（上交所）按天查询；深市（深交所 `fund_jjgm`）按 ~90 天分段查询+翻页（翻页参数为全大写 `PAGENO`，每页 20 条），两市均支持全量历史日频数据，已回补 2024-01-02 至今。日常刷新用默认窗口即可。
- **限流容错**：K 线优先级为 新浪 → 东财 → 腾讯，某源连续失败 2 次后本轮自动跳过；指数成交额优先搜狐（不限流）、失败退东财，两者都不影响收盘价数据。

## 国家队披露数据维护

披露数据存入 `disclosures.json`（按 ETF 代码组织，每条含
`report_date / combined_ratio_pct / combined_value_yi / held_units_yi_qfq / total_shares_yi_qfq / by_body_pct`），
下次刷新自动合并进页面（含"最新持仓比例/金额"和份额图上的披露标记点）。
`by_body_pct` 记录各主体（汇金投资/汇金资管/国新/诚通）的占比明细。

**每期年报/半年报披露后（约 3 月底、8 月底）的更新流程：**
1. 跑 `python scan_market.py` 全市场复扫（新浪十大持有人接口探测，约 3–5 分钟），比对 `scan_candidates.json` 找新增/退出；
2. 用交易所官方总份额交叉验证报告期；
3. 更新 `disclosures.json`（精确份额、占比、按官方净值计算金额）；
4. 跑 `python refresh_data.py` 合并。

## 文件清单

| 文件 | 作用 |
|---|---|
| `index.html` / `styles.css` / `app.js` | 前端应用 |
| `data.js` / `data.json` | 应用数据（刷新脚本产物） |
| `refresh_data.py` | 真实数据刷新脚本（零第三方依赖，含 ETFS 清单） |
| `serve.py` | 本地服务 + `/api/refresh` 一键刷新接口 |
| `start_server.bat` / `stop_server.bat` | 双击启动 / 双击关闭本地服务 |
| `disclosures.json` | 国家队披露数据（人工维护，报告期 2025-12-31） |
| `scan_market.py` | 全市场 ETF 国家队持仓扫描脚本（每期披露后复扫用） |
| `scan_candidates.json` | 扫描候选清单（含诚通系 15 只与口径修正说明） |
| `scan_market.json` | 最近一次全市场扫描原始结果存档 |
| `kline_cache.json` / `shares_cache.json` | 增量缓存（⚠️ 勿删，删后需重跑约 25 分钟全量回补） |
